"""Live aquarium lab (P6): run whites from t=0 in front of a person, branch them into parallel universes,
and talk about them with an AI guide. The physics is the unchanged model code in genesis/models and core/;
this package only wraps it, records every knob/perturbation as something PUT IN, and streams frames.

    python -m tools.lab.server            # http://127.0.0.1:8765  (see docs/LAB.md)
"""
