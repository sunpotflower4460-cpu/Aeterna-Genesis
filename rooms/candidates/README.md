# rooms/candidates/ — AI 候補 Room（非公式・パラレル宇宙）

AI Genesis Lab（`ai_lab/lab.py --promote-best`）が親 Room の始原条件を変えて **2D→local-3D** まで通した候補を、
**official と区別して**ここに保存する（`ROOM_MODEL.md` のパラレル宇宙・上書き禁止）。

- `official: false` / `status: candidate` / `dimension_status.full_3d: not_started`。
- **full-3D 格子収束・複数 seed・物理監査を通していない**＝正式 Room ではない。
- 親 Room（`rooms/official/room-g001-a`）は**変更しない**。候補は別 Room として積む。
- 正式化は昇格段階（coarse-global-3d → full-3d → physics audit）を経て人/コマンドが行う。
