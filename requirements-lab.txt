# Optional: the AI council of the live aquarium lab (tools/lab/council.py). Install only what you use.
anthropic       # core: Claude Opus 5.5
openai          # only if GPT is used (DeepSeek needs nothing: it is called directly with the standard library)
google-genai    # Gemini (reads images and video)
