# P07 / E候補（結果は role N）。Hand-authored to match schemas/experiment.schema.json. 既存 results 不変更。
# 一次情報: docs/frontier/F0_P07_addendum_grown_slow_field.md（本実験の事前追記）＋ F0_P07_field_slow_field_basin.md。
# e049(S1)/e050(S2) の hysteresis 過剰を「記憶」と呼べるかを瞬時コピー対照で検定 → copy-collapse（記憶でなく結合再規格化）。
id: e051
title: 育つ遅い場は記憶か（P07/E候補）— 未分化 s(t0)=0 の hysteresis 過剰は瞬時コピーで再現＝copy-collapse
role:
  primary: N
  secondary:
  - S
confidence: B
claim_tier:
- measured
- observed
put_in: e049/e050 と同一の局所 GL＋遅い場（双方向局所結合）。s(t0)=0（未分化・grown）／構造テンプレ（placed 参照）／瞬時コピー s=|psi|^2（tau→0, copy）／g=0（OFF）を全て一致条件で比較
emerged: 育つ grown-s は excess>margin を出すが、瞬時コピー（tau→0, 結合はあるが lag=0）も同じ excess を出す（history_gain=grown−copy=0.012≦margin）。過剰は遅い場の「記憶(lag)」でなく結合の再規格化（有効飽和 (1−g)|psi|^2）。E候補は不支持。s の空間構造自体は 0→有意に育つが、それは記憶を担っていない
seeded_structure: true
dimension:
  computed: mixed
  official_3d: false
  transfer_risk: medium
genesis_role: negative_constraint
target_encoded: false
unresolved_audit:
- role N：E候補（s が未分化から育った記憶）は不支持。excess は copy(tau→0)で再現されるため lag-記憶でない
- この結果は e049/e050 の「basin memory」解釈を限定する：ON−OFF 過剰は結合再規格化を含む。真の記憶主張には瞬時コピー対照が必須（本実験が追加）
- lag が寄与する掃引率・tau レジーム（quench 型・非準静的）や別 basin 観測量では history_gain>0 になりうる（残課題）
known_match: 遅い変数の hysteresis と、有効ポテンシャル再規格化の区別（材料 plasticity vs 単なる非線形係数変化）。matched instantaneous-copy 対照
results:
- experiments/e051_grown_slow_field/results/grown_slow_field_memory.json
audit:
  physics_integrity: passed
  no_target_encoding: passed
  conservation: passed
  convergence: passed
  reproducibility: passed
  visualization_integrity: not_applicable
