# P07 / S2 (role S). Hand-authored to match schemas/experiment.schema.json. 物理コード追加、既存 results は不変更。
# 一次情報は docs/frontier/F0_P07_field_slow_field_basin.md。e049(S1) の robustness+3D 昇格。E候補（s が0から創発）は別実験。
id: e050
title: 遅い場 basin 記憶の robustness＋3D昇格（P07/S2・g帯・解像度・seed・2D→3D）
role:
  primary: S
  secondary:
  - N
confidence: B
claim_tier:
- measured
- observed
put_in: e049 と同じ局所 GL＋遅い場（双方向局所結合）。g・解像度 L・seed・次元(2D/3D)を振る。s に目標は置かない
emerged: hysteresis 過剰（ON−OFF）が g に onset(≈0.3)を持ち単調増、解像度 L=32/48/64 で安定（有限サイズでない）、複数 seed で mean−std>margin、2D→3D で生存（3D excess>margin・エネルギー有界）。robust な slow-field basin 記憶候補
seeded_structure: true
dimension:
  computed: mixed
  official_3d: false
  transfer_risk: medium
genesis_role: design_hypothesis
target_encoded: false
unresolved_audit:
- role S：s も ψ も置く。s 自体が未分化から自然発生するか（E候補）は e051 で別途
- 3D は L=24³・単一 g。full-3D Room 化・広い相図・大 τ 依存は残課題
- official_3d=false（screen 段階の 3D 生存確認）。正式3D Room 昇格は別監査
known_match: 遅いフィードバックの双安定/hysteresis（材料 plasticity・遅い秩序変数）／臨界緩和の掃引率 confound（OFF で定量）
results:
- experiments/e050_field_slow_field_basin_s2/results/basin_memory_robustness.json
audit:
  physics_integrity: passed
  no_target_encoding: passed
  conservation: passed
  convergence: passed
  reproducibility: passed
  visualization_integrity: not_applicable
