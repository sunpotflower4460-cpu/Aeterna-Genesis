{
  "version": 1,
  "mission_id": "zero-to-division-like-v1",
  "title": "Strict minimal start to persistent individual self-separation",
  "description": "Find a non-seeded route from a strict minimal start to a persistent individual-like structure that later becomes two persistent descendants with accounting and continuity evidence. This is a research mission, not a biological cell-division claim.",
  "start_policy": {
    "strict_goal_start": "Z-A:minimal-white",
    "scaffolded_start_can_satisfy_strict_goal": false,
    "correlated_random_can_satisfy_strict_goal": false,
    "record_non_strict_routes_as_useful_leads": true
  },
  "requirements": [
    {"id": "strict_start_purity", "label": "Strict minimal white/Z-A start is verified", "required": true},
    {"id": "individual_emerged", "label": "One persistent individual-like body emerges after t=0", "required": true},
    {"id": "pre_split_persistence", "label": "The same body persists for a minimum audited interval before separation", "required": true},
    {"id": "identity_continuity", "label": "Evidence supports continuity of the same body into the separation event", "required": true},
    {"id": "one_to_two_transition", "label": "One persistent body becomes two separately trackable bodies", "required": true},
    {"id": "daughter_persistence", "label": "Both descendants persist after separation", "required": true},
    {"id": "accounting", "label": "Relevant conserved or tracked quantities are accounted for across the transition", "required": true},
    {"id": "inheritance", "label": "At least one pre-separation internal feature is audibly/quantitatively tracked into both descendants", "required": true},
    {"id": "fresh_seed_reproduction", "label": "The route reproduces with independent fresh seeds", "required": true},
    {"id": "prefix_integrity", "label": "Long-horizon evidence passes Prefix Identity Audit", "required": true}
  ],
  "forbidden_shortcuts": [
    "seed target morphology",
    "seed pair or triangle geometry",
    "seed vortex positions or charges",
    "seed division location or division time",
    "change success thresholds to fit a candidate",
    "treat F7 or network separation alone as biological cell division"
  ],
  "changes_official_levels": false,
  "changes_official_rooms": false
}
